service: rest-api
frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  profile: ${opt:profile, "serverless-admin"}
  region: ${opt:region, "ca-central-1"} # sls deploy --region us-east-1
  stage: ${opt:stage, "dev"}
  lambdaHashingVersion: 20201221

functions:
  userapi:
    handler: api.handler
custom:
  DynamoDBTableName: ${self:provider}-users-${self:provider.stage}
resources:
  Resources:
    usersDynamoDbTable:
      Type: AWS::DynamoDB::Table
      Properties:
        AttributeDefinitions:
          - AttibeuteName: user-id
            AttributeType: N
        KeySchema:
          - AttribeuteName: user_id
            AttributeType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: "5"
          WriteCapacityUnits: "5"
        TableName: ${self:custom.DynamoDBTableName}
